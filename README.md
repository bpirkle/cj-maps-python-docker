# cj-maps-python-docker
Fiddling around with Docker and Python for a maps project

Default port is 8080.
Create a .env file to override that. Example:
CJ_DOCKER_PORT=8089


http://localhost:8089
docker compose up -d --build
docker stop
docker down